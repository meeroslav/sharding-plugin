# jest-sharded-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build jest-sharded-plugin` to build the library.

## Running unit tests

Run `nx test jest-sharded-plugin` to execute the unit tests via [Jest](https://jestjs.io).
